<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.timeshards.mapper.ConfigMapper">

    <resultMap id="BaseResultMap" type="com.timeshards.entity.Config">
        <id column="id" property="id" />
        <result column="config_key" property="configKey" />
        <result column="config_value" property="configValue" />
        <result column="description" property="description" />
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, config_key, config_value, description FROM sys_config
    </select>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT id, config_key, config_value, description FROM sys_config WHERE config_key = #{configKey}
    </select>

    <!-- 采纳你的 insert -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config (config_key, config_value, description)
        VALUES (#{configKey}, #{configValue}, #{description})
    </insert>

    <update id="updateValue">
        UPDATE sys_config SET config_value = #{configValue} WHERE config_key = #{configKey}
    </update>

</mapper>