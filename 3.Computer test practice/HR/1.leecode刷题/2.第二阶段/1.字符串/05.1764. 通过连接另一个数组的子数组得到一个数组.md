# [1764. 通过连接另一个数组的子数组得到一个数组](https://leetcode.cn/problems/form-array-by-concatenating-subarrays-of-another-array/)

中等

提示

给你一个长度为 `n` 的二维整数数组 `groups` ，同时给你一个整数数组 `nums` 。

你是否可以从 `nums` 中选出 `n` 个 **不相交** 的子数组，使得第 `i` 个子数组与 `groups[i]` （下标从 **0** 开始）完全相同，且如果 `i > 0` ，那么第 `(i-1)` 个子数组在 `nums` 中出现的位置在第 `i` 个子数组前面。（也就是说，这些子数组在 `nums` 中出现的顺序需要与 `groups` 顺序相同）

如果你可以找出这样的 `n` 个子数组，请你返回 `true` ，否则返回 `false` 。

如果不存在下标为 `k` 的元素 `nums[k]` 属于不止一个子数组，就称这些子数组是 **不相交** 的。子数组指的是原数组中连续元素组成的一个序列。

**示例 1：**

```
输入：groups = [[1,-1,-1],[3,-2,0]], nums = [1,-1,0,1,-1,-1,3,-2,0]
输出：true
解释：你可以分别在 nums 中选出第 0 个子数组 [1,-1,0,1,-1,-1,3,-2,0] 和第 1 个子数组 [1,-1,0,1,-1,-1,3,-2,0] 。
这两个子数组是不相交的，因为它们没有任何共同的元素。
```

**示例 2：**

```
输入：groups = [[10,-2],[1,2,3,4]], nums = [1,2,3,4,10,-2]
输出：false
解释：选择子数组 [1,2,3,4,10,-2] 和 [1,2,3,4,10,-2] 是不正确的，因为它们出现的顺序与 groups 中顺序不同。
[10,-2] 必须出现在 [1,2,3,4] 之前。
```

**示例 3：**

```
输入：groups = [[1,2,3],[3,4]], nums = [7,7,1,2,3,4,7,7]
输出：false
解释：选择子数组 [7,7,1,2,3,4,7,7] 和 [7,7,1,2,3,4,7,7] 是不正确的，因为它们不是不相交子数组。
它们有一个共同的元素 nums[4] （下标从 0 开始）。
```

**提示：**

- `groups.length == n`
- `1 <= n <= 103`
- `1 <= groups[i].length, sum(groups[i].length) <= 103`
- `1 <= nums.length <= 103`
- `-107 <= groups[i][j], nums[k] <= 107`

```python
class Solution:
    def canChoose(self, groups: List[List[int]], nums: List[int]) -> bool:
        count = 0
        i = 0
        m = len(groups[-1])
                # 6     3
        while i <= len(nums)-m:
            if count < len(groups):
                k = len(groups[count])
                if nums[i:i+k] == groups[count]:
                    count += 1
                    i += k
                else:
                    i += 1
        if count == len(groups):
            return True
        return False    
```

```python
class Solution:
    def canChoose(self, groups: List[List[int]], nums: List[int]) -> bool:
        count = 0
        i = 0
        m = len(groups[-1])
                # 6     3
        while i <= len(nums)-m:
            if count < len(groups):
                k = len(groups[count])
                if nums[i:i+k] == groups[count]:
                    count += 1
                    i += k
                    if count == len(groups):
                        return True
                else:
                    i += 1
        if count == len(groups):
            return True
        return False

        
```

你的这段代码逻辑大体上是正确的，能够通过这道题（Accept），利用了 Python 的切片特性使得代码写起来比较简短。

不过，从**算法逻辑严密性**、**代码执行效率**以及**通用性**的角度来看，还有不少可以优化和改进的地方。

我给你的代码打分：**80分**。

以下是详细的点评和改进建议：

### 1. 代码分析与点评

#### ✅ 优点：
1.  **贪心思想正确**：你使用了双指针（`i` 遍历 `nums`，`count` 遍历 `groups`），这符合贪心算法的思路——尽可能早地匹配当前 group，匹配不到就往后移一格，匹配到了就跳过整段。
2.  **利用 Python 特性**：使用了 `nums[i:i+k] == groups[count]` 进行列表比对，代码非常直观。
3.  **边界处理**：虽然逻辑有点奇怪（见下文），但最终没有出现数组越界的问题。

#### ⚠️ 问题点：

**1. `m = len(groups[-1])` 的定义非常具有误导性**
*   **代码位置**：`while i <= len(nums)-m:`
*   **问题**：你试图通过减去“最后一个 group 的长度”来控制循环结束条件。但这在逻辑上是不严谨的。
    *   如果我们当前正在找的是 `groups[0]`，它的长度可能比 `groups[-1]` 大得多。依靠 `m` 来控制循环，可能会导致在其他语言（如 C++ 或 Java）中发生数组越界，或者逻辑混乱。
    *   **为什么你在 Python 里没报错？** 因为 Python 的切片是非常宽容的。即使 `i+k` 超过了数组长度，`nums[i:i+k]` 也不会报错，只会返回一个较短的列表，然后比对失败。这属于“利用了语言特性的‘巧合’”，而不是严密的逻辑。

**2. 缺少“提前退出”机制 (效率问题)**
*   **代码位置**：`if count < len(groups): ... else: i += 1`
*   **问题**：假设 `nums` 很长，但我们早在第 5 个位置就找到了所有的 `groups`。你的代码中 `count` 已经等于 `len(groups)` 了，但 `while` 循环依然会傻傻地执行，直到 `i` 走到数组末尾。
*   **建议**：一旦 `count == len(groups)`，应该直接 `return True`。

**3. `else` 分支的缩进逻辑**
*   你的逻辑是：如果没找到，`i += 1`。
*   但在循环内部，当 `count` 已经找完所有 group 时，你的代码会进入 `else` 分支让 `i` 继续空转。虽然不影响结果，但看起来很奇怪。

### 2. 改进后的代码（推荐写法）

这是优化后的版本，逻辑更清晰，去掉了奇怪的 `m`，增加了提前退出：

```python
class Solution:
    def canChoose(self, groups: List[List[int]], nums: List[int]) -> bool:
        n = len(nums)
        i = 0  # nums 的指针
        g_idx = 0  # groups 的指针 (你代码里的 count)
        
        while i < n and g_idx < len(groups):
            # 获取当前要找的子数组的长度
            k = len(groups[g_idx])
            
            # 1. 检查剩余长度够不够 (严谨的边界检查)
            if i + k > n:
                return False
            
            # 2. 尝试匹配
            if nums[i : i+k] == groups[g_idx]:
                g_idx += 1  # 找到了，找下一个 group
                i += k      # 指针跳过当前这个子数组 (不相交)
            else:
                i += 1      # 没找到，nums 指针往后挪一格
        
        # 循环结束后，检查是否找齐了所有的 group
        return g_idx == len(groups)
```

### 3. 核心改进点对比

| 特性         | 你的写法                                        | 改进后的写法                                               |
| :----------- | :---------------------------------------------- | :--------------------------------------------------------- |
| **循环条件** | `i <= len(nums)-m` (依赖最后一个组长，逻辑略偏) | `i < n and g_idx < len(groups)` (标准双指针)               |
| **提前结束** | **无** (找完了也会空转到最后)                   | **有** (一旦找完，循环条件 `g_idx < len` 不满足，直接退出) |
| **边界检查** | 依赖 Python 切片的容错性                        | 显式检查 `i + k > n`，逻辑更严密                           |
| **可读性**   | 较好，但 `m` 让人困惑                           | 清晰，符合通用算法逻辑                                     |

### 4. 总结
这道题其实就是模拟题，你的解法在 Python 环境下完全没问题，是合格的解法。但如果在面试中，面试官可能会挑战你 `m` 的含义以及为什么不提前退出。改用上面的写法会显得你对边界条件和效率控制得更专业。
