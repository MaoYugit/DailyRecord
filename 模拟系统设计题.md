前端系统设计题（Frontend System Design）与后端不同，它更侧重于**性能优化、组件架构、状态管理、用户体验和数据流控制**。

作为实习生，面试官通常不指望你给出专家级的方案，但希望看到你**考虑问题的全面性**（比如：网络慢怎么办？数据多了怎么办？）。

以下是10个常见的模拟设计题，按难度和类型排序：

---

### 1. 设计一个“虚拟列表”组件 (Virtual List)
*   **场景：** 页面需要渲染 10 万条数据（如聊天记录或订单列表），直接渲染会导致页面卡死。
*   **考察点：** DOM 性能优化。
*   **核心关键词：** 可视区域计算、滚动偏移（offset）、缓冲区（buffer）、高度计算。

### 2. 设计一个大文件上传系统
*   **场景：** 用户需要上传一个 2GB 的视频文件，要求支持断点续传。
*   **考察点：** 浏览器 API 应用与健壮性。
*   **核心关键词：** 文件分片（Blob.slice）、计算 Hash (SparkMD5)、并发控制、秒传（校验 Hash）、断点记录（localStorage）。

### 3. 设计一个图片懒加载与预加载系统
*   **场景：** 一个类似小红书的瀑布流页面，图片非常多。
*   **考察点：** 资源加载策略与体验优化。
*   **核心关键词：** `IntersectionObserver`、占位图（Skeleton屏/低清图）、骨架屏、浏览器缓存机制。

### 4. 设计一个搜索框的输入联想（Autocomplete）
*   **场景：** 百度或 Google 的搜索框，输入时实时显示结果。
*   **考察点：** 用户体验与接口压力控制。
*   **核心关键词：** 防抖（Debounce）、竞态处理（如果请求 A 比请求 B 晚返回但 B 是后发的，如何处理？）、缓存搜索结果。

### 5. 设计一个前端监控与异常上报系统 (SDK)
*   **场景：** 公司需要监控所有页面的报错、性能指标和用户行为。
*   **考察点：** 系统健壮性与无侵入式设计。
*   **核心关键词：** `window.onerror`、`unhandledrejection`、Performance API、Beacon API (离线/销毁前上报)、分片上报。

### 6. 设计一个全局状态管理方案
*   **场景：** 一个复杂的大型 App（如外卖 App），多个页面需要共享用户信息、购物车、定位等。
*   **考察点：** 数据流向与代码组织。
*   **核心关键词：** 单向数据流、模块化（Store Modules）、持久化存储（localStorage 同步）、响应式原理。

### 7. 设计一个“离线优先”的笔记应用
*   **场景：** 类似你做的经期 App，用户在地铁没信号时也能记录，连网后同步。
*   **考察点：** 本地存储与同步逻辑。
*   **核心关键词：** Service Worker (PWA)、IndexedDB、冲突解决策略（时间戳）、本地队列。

### 8. 设计一个主题定制/换肤系统
*   **场景：** 支持一键切换“少女粉”、“暗黑模式”或“品牌定制色”。
*   **考察点：** CSS 架构能力。
*   **核心关键词：** CSS Variables (变量)、Runtime 修改、类名切换、预处理器（Sass/Less）的配置。

### 9. 设计一个权限管理系统（RBAC）
*   **场景：** 后台管理系统，不同职位的员工看到不同的菜单、按钮和页面。
*   **考察点：** 安全性与架构设计。
*   **核心关键词：** 动态路由（addRoute）、指令级控制（v-permission）、后端返回权限列表、路由守卫。

### 10. 设计一个消息推送与实时聊天通知
*   **场景：** 类似微信，App 处于后台或打开状态时，实时接收消息。
*   **考察点：** 双工通信。
*   **核心关键词：** WebSocket、心跳检测（Heartbeat）、断线重连、消息去重。

### 11. 设计一个实时协作的在线文档

---

### 面试回答模板（万能公式）：

当你遇到这些设计题时，按这个步骤说，面试官会觉得你很有逻辑：

1.  **需求分析 (Clarification)**：先问清楚场景（“是移动端还是 PC？”、“数据量大概多大？”）。
2.  **核心模块 (Core Components)**：把 App 拆成几个部分（“UI 层、数据处理层、存储层”）。
3.  **技术方案 (Technical Choices)**：说明你为什么选这个方案（“因为 uniapp 方便跨端”、“因为 IndexedDB 容量大”）。
4.  **难点攻克 (Bottlenecks)**：主动提出潜在问题（“如果网络断了怎么办？”、“如果用户快速点击怎么办？”）。
5.  **总结优化**：后续可以怎么升级（“加缓存、加埋点、做性能监控”）。

**你可以挑选其中一个你最感兴趣或者觉得最难的，我可以模拟面试官给你详细拆解一下思路！**

好的，针对前端面试，除了之前提到的性能优化和底层技术，面试官还经常会考察你对**工程化、组件化方案、用户体验细节以及前后端协作**的理解。

这里为你补充另外 10 个常见的系统设计题，这些题目在鹅厂、阿里、字节等大厂的面试中非常高频：

---

### 11. 设计一个全局通用的“弹窗/消息通知”系统 (Message/Notification)
*   **场景：** 在应用的任何地方（JS 代码里或组件里）都能通过 `Message.success('保存成功')` 调用。
*   **考察点：** 单例模式、组件命令式调用、层级管理（Z-index）。
*   **核心关键词：** 动态创建挂载点（`document.body.appendChild`）、队列管理（同时弹出多个消息怎么排列）、Vue 的 `h` 函数或 `createVNode`。

### 12. 设计一个“动态表单”引擎 (Schema-driven Form)
*   **场景：** 后端返回一个 JSON 配置，前端自动渲染出包含输入框、下拉框、校验规则的表单。
*   **考察点：** 配置化思想、递归组件、表单状态收集。
*   **核心关键词：** JSON Schema、状态双向绑定、动态验证规则、复杂布局映射。

### 13. 设计一个“无限滚动”瀑布流 (Infinite Scroll)
*   **场景：** 类似抖音或小红书的下拉加载。
*   **考察点：** 滚动监听、接口分页、防止重复请求。
*   **核心关键词：** `window.onscroll` vs `IntersectionObserver`、底部加载中状态、分页逻辑（page/pageSize）、请求锁（loading 状态）。

### 14. 设计一个前端“国际化”方案 (i18n)
*   **场景：** 应用需要支持中、英、日三种语言切换。
*   **考察点：** 语言包组织、动态切换而不刷新页面。
*   **核心关键词：** 语言配置文件、占位符替换、本地缓存（记住上次选的语言）、动态导入（懒加载语言包以减小体积）。

### 15. 设计一个“暗黑模式”与“主题定制”系统
*   **场景：** 跟随系统自动切换深浅色模式，或由用户手动选择。
*   **考察点：** 样式管理方案、CSS 变量应用。
*   **核心关键词：** CSS Variables (`--main-color`)、`matchMedia('(prefers-color-scheme: dark)')`、属性选择器（`data-theme='dark'`）、持久化存储。

### 16. 设计一个“全站搜索”防抖与缓存系统
*   **场景：** 在顶部搜索框输入时，实时展现聚合搜索结果。
*   **考察点：** 竞态处理（Race Conditions）、性能优化。
*   **核心关键词：** 防抖（Debounce）、AbortController（取消上一次未完成的请求）、内存缓存（Map 存储已搜索过的词）、搜索词高亮处理。

### 17. 设计一个“登录授权与 Token 刷新”机制
*   **场景：** Token 有效期短（如 2 小时），如何在不让用户重新登录的情况下静默刷新。
*   **考察点：** 网络请求拦截器（Interceptor）、安全性。
*   **核心关键词：** JWT (Access Token / Refresh Token)、Axios 请求拦截与响应拦截、无感知刷新（在响应拦截器里发现 401 后先挂起请求，刷新后再重发）。

### 18. 设计一个“骨架屏”生成方案 (Skeleton Screen)
*   **场景：** 在数据返回前，展示页面的灰色轮廓以降低用户焦虑。
*   **考察点：** 用户感知性能优化。
*   **核心关键词：** 自动化工具生成（如 `page-skeleton-webpack-plugin`）、手动封装骨架组件、SVG vs CSS Animation。

### 19. 设计一个“前端埋点与行为追踪” SDK
*   **场景：** 需要统计用户点击了哪个按钮、在某个页面停留了多久。
*   **考察点：** 无侵入式埋点、数据上报策略。
*   **核心关键词：** 指令式埋点（v-track）、全埋点（监听所有 click 事件）、数据合并上报（每 5 条报一次）、离线存储。

### 20. 设计一个“多标签页/多窗口”通信方案
*   **场景：** 用户在一个标签页退出登录，另一个已打开的标签页也需要自动退出。
*   **考察点：** 同源策略下的跨标签通信。
*   **核心关键词：** `BroadcastChannel`、`localStorage` 事件（`onstorage`）、`SharedWorker`、窗口引用（`window.opener`）。

---

### 面试深度拔高建议：

如果你想在实习生面试中脱颖而出，回答这些问题时可以主动提到：

1.  **容错处理：** “如果接口报错了，我的系统会如何 fallback（降级处理）？”
2.  **安全性：** “在做本地存储时，我会考虑是否需要对敏感信息加密。”
3.  **可维护性：** “我会将业务逻辑和 UI 逻辑解耦，方便后续写单元测试。”
4.  **结合你的项目：** 比如你正在做的**经期管理 App**：
    *   *“在设计这个 App 时，我考虑到了第 17 题的本地存储方案，为了保护女性隐私，我没有设计后端，而是采用了纯前端加密存储，这借鉴了系统设计中的‘隐私安全优先’原则。”* 

这样回答，面试官会觉得你不仅会写代码，还有**系统性思维**。加油！